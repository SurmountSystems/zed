# agent

Native agent runtime for Zed, including the surmount fork's Grok Build profile (`is_grok_build_profile`), native tools (`todo_write`, `monitor`, plan tools), and thread persistence.

Fork-specific maintainer notes: [SURMOUNT.md](../../SURMOUNT.md) — Native agent core, Agent tools, Datastore sections.