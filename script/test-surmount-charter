#!/usr/bin/env bash
# Native Grok Build completion charter (SURMOUNT.md pillars 1–6).
# Single regression surface — separate from agent_ui immersive cold-start tests.
set -euo pipefail
cd "$(dirname "$0")/.."
exec cargo test -p agent native_grok -- "$@"